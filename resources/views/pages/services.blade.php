{{-- @extends('layouts.app')

@section('content')
    <h1>
    </h1>

    @if(count($services) > 0)
        @foreach ($services as $service)
            <li>
            </li>
        @endforeach
    @endif
@endsection --}}